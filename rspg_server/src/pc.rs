// Player structure
// Contains information about the player
#[derive(Debug)]
pub struct Player {
    pub first_name: String,
    pub last_name: String,
    pub email: String,
    pub character: String,
}
